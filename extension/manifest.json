{
  "manifest_version": 3,
  "name": "Slack Notification Onboarding",
  "version": "1.0.0",
  "description": "Adds notification preference onboarding when you first use Slack - choose how you want to be notified (DMs only, mentions only, everything, or off).",
  "permissions": ["storage"],
  "host_permissions": ["https://app.slack.com/*", "https://*.slack.com/*"],
  "content_scripts": [
    {
      "matches": ["https://app.slack.com/*", "https://*.slack.com/client/*"],
      "js": ["content.js"],
      "css": ["content.css"],
      "run_at": "document_idle"
    }
  ]
}
