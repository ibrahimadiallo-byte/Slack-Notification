/**
 * Interface layer – app UI entry point.
 * Depends on core for types, state, and services.
 */

export { OnboardingNotificationScreen } from './screens';
export { Button, NotificationOptionCard } from './components';
