# State

Application and onboarding state.

- Whether the user has completed the notification onboarding step
- Selected notification preference
- Persistence (e.g. local storage / user settings API) for "you can change this later in Settings"
