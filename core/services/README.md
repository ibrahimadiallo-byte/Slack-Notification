# Services

Business logic and external integration.

- **notificationService** – Requesting system notification permission, applying preference (DMs only, mentions only, everything, off)
- **onboardingService** – Recording that onboarding step is done, fetching/saving user preference
- API or workspace integration as needed
